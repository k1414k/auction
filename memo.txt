NEXTAPI 通信例 = async (e:React.FormEvent<HTMLFormElement>) => {
    e.preventDefault()
    setErrors("")
    try {
      await nextApi("/auth/sign-in", {
        method: "POST",
        body: form,
      })
      router.replace('/')
    }
    catch (e){
      if (e instanceof Error){
        const errorMessage= JSON.parse(e.message)
        setErrors(errorMessage.message)
      }
      else {
        setErrors("ログイン失敗しました。")
      }
    }
  }


フロント画面作る：検索コンポーネントとページ、出品ページ（プロダクトのcreate部分）、閲覧ページ

会員登録のときrails側のルールを知ってフロントに合わせることにより正確なバリデーションを行う
（今だとなんでも被るユーザーと知らせてる）


商品機能
０．カテゴリー
１．価格
２．入札　ー　ユーザー、メッセージ
３．チャート
４．期限
５．写真

メッセージ機能

デプロイメモ；
rails側のサーバー閉ざされたり問題ある時にreact側も５００などにするべき






