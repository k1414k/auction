# 未実装・不完全機能 調査レポート

## 調査概要

* 調査したページ・画面
  * ユーザー向け: `frontend/src/pages/index.tsx`, `search.tsx`, `favorites.tsx`, `sell.tsx`, `notifications/index.tsx`, `items/[id]/index.tsx`, `items/[id]/checkout.tsx`, `transaction/[id]/index.tsx`, `transaction/[id]/rating.tsx`, `user/profile.tsx`, `user/items.tsx`, `user/wallet.tsx`, `auth/*`
  * 共通UI: `frontend/src/components/PCNav.tsx`, `BottomNav.tsx`, `SearchHeader.tsx`, `SearchTop.tsx`, `CategoryChips.tsx`, `SelectRow.tsx`, `Modal.tsx`
  * API接続: `frontend/src/pages/api/**`, `backend/config/routes.rb`, `backend/app/controllers/auction/v1/**`, `backend/app/controllers/admin/v1/**`
  * 管理画面: `admin/src/pages/dashboard.vue`, `users.vue`, `categories.vue`, `items.vue`, `orders.vue`, `settings.vue`, `admin/src/components/layouts/AdminLayout.vue`
* 確認したメニュー・ボタン・リンク
  * グローバルナビ: ホーム、探す、出品、いいね、マイページ
  * マイページ: 入札中、出品中、購入履歴、販売履歴、ポイント履歴、住所変更、パスワード変更、ログアウト、ウォレット、商品管理
  * 商品導線: いいね、入札する、値段交渉、購入手続き、配送先選択、支払い方法、チャージ、購入確定、取引メッセージ、評価
  * 管理画面: ダッシュボード、ユーザー、カテゴリ、商品、注文、設定、通知アイコン、検索アイコン
* 調査日時または作業時点のメモ
  * 2026-06-20 23:39 JST 時点のコード調査。
  * 今回は機能実装、API追加、バックエンド変更、DB変更は行わず、コード上のUI表示とAPI/状態管理の接続状況を確認した。

## 未実装または不完全な可能性がある機能一覧

| 画面 | 表示名 | 現在の状態 | 問題点 | 推奨対応 |
| -- | --- | ----- | --- | ---- |
| マイページ | 入札中 | タブは存在するが「入札履歴は準備中です」と表示 | ユーザー自身の入札一覧APIや状態管理との接続が見当たらない | 将来、`/user/bids` 相当のAPI、一覧UI、落札/失注状態の整理を検討 |
| マイページ | ポイント履歴 | タブは存在するが「売上高・ポイントの履歴は準備中です」と表示 | 現在残高は表示されるが、履歴テーブル/API/モデルは見当たらない | ポイント・売上移動の履歴モデル/API/表示仕様を定義 |
| マイページ | 住所変更 | ボタン表示のみ | `onClick` や遷移先がなく、押しても動作しない | 住所管理画面に遷移するか、既存の住所モーダルをプロフィール側にも接続 |
| マイページ | 自己紹介編集 | クリックでtextareaは出る | 保存ボタン、値の保持、更新API接続がない | `introduction` 更新APIと保存/キャンセルUIを整理 |
| マイページ | 購入履歴 | `/orders?role=buyer` に接続 | 注文データの表示はあるが、空の場合は「履歴なし」。仕様として購入履歴の範囲が注文のみでよいか要確認 | 実装済み寄り。履歴定義、表示項目、空状態文言を確認 |
| マイページ | 販売履歴 | `/orders?role=seller` に接続 | 注文データの表示はあるが、販売完了・取引中の扱いがUI上で未整理 | 実装済み寄り。販売履歴と取引中一覧を分けるか確認 |
| マイページ/商品管理 | 出品中・商品管理 | `/user/items` に接続し、一覧/削除は動作する構成 | 「詳細」リンクは商品詳細へ遷移するだけで、編集画面はない | 商品編集が必要なら編集UIと既存PATCH APIの接続を検討 |
| いいねページ | 閲覧履歴 | タブはあるが開発中メッセージのみ | 閲覧履歴の保存/取得APIや状態管理が見当たらない | ローカル履歴かサーバー履歴か仕様決定後に実装 |
| いいねページ | フィルタアイコン | ボタン表示のみ | `SlidersHorizontal` ボタンにクリック処理がない | フィルタ機能を作るか、未実装なら表示を見直す |
| お知らせページ | お知らせ一覧 | 静的なサンプル通知を表示 | 通知API、既読状態、詳細リンク、タブ切替がない | 通知仕様/APIを定義。未実装中は導線表示を制御 |
| お知らせページ | すべて/やることリスト | ボタン表示のみ | タブ切替の状態管理やデータ接続がない | タブごとの通知カテゴリを実装するか表示を整理 |
| 認証 | パスワード再設定 | 入力フォームと送信ボタンのみ | `onSubmit` やNext API/バックエンド接続が見当たらない | Devise Token Authの再設定フローに合わせてAPI接続 |
| 商品詳細 | オファー送信 | `/items/:id/offers` にPOSTして送信可能 | 出品者がオファーを一覧・承認・拒否するUIが見当たらない | 受信オファー一覧、承認/拒否導線、購入/取引化の仕様確認 |
| 商品詳細/マイページ | 入札関連 | 商品単位の入札作成APIはある | 自分の入札一覧、入札中商品の追跡、オークション終了後の落札処理が見当たらない | 入札履歴、落札判定、注文作成フローを設計 |
| 購入確認 | クレジットカード | 「現在取り扱っておりません」と表示し選択不可 | 決済API接続はポイントのみ | 表示は明示的。カード決済を提供する場合のみ実装 |
| 取引評価 | 評価投稿 | UI上は評価とコメント入力がある | 実際は注文ステータスを`completed`にするだけで、評価値/コメントは送信・保存されない | 評価モデル/APIを追加するか、現状に合わせて文言を変更 |
| グローバルナビ | PCNavの入札/落札数ティッカー | 数値がハードコード | 実データAPIに接続されていない | ダッシュボード/集計APIに接続するか、表示の扱いを再検討 |
| 管理画面 | 注文 | `/orders` ページは「準備中」 | 管理画面メニューとダッシュボードの「すべて表示」から遷移できるが中身がない | 管理用注文一覧API/UIを実装するかメニュー表示を制御 |
| 管理画面ダッシュボード | 売上推移 | プレースホルダー表示 | グラフ実装や時系列APIがない | 売上集計仕様とグラフ実装を検討 |
| 管理画面ダッシュボード | 最近の活動 | 「デバイスログイン」が静的表示 | 活動ログAPIや実データ接続がない | 活動ログを実装するか表示を削除/準備中化 |
| 管理画面ダッシュボード | 成長率 | APIレスポンスが全て`0.0` | 実際の期間比較集計ではない | 比較期間と集計ロジックを定義 |
| 管理画面トップバー | 通知/検索アイコン | アイコンボタンのみ | クリック処理や検索/通知UIがない | 機能を作るまで非表示または準備中扱いにする |
| 管理画面設定 | アカウント設定/パスワード変更 | フォーム表示のみ | 入力値、保存処理、API接続がない | 設定更新APIとフォーム状態を実装するか、未実装表示に変更 |
| 管理画面設定 | 通知設定/セキュリティ/バックアップ | リスト表示のみ | 遷移先や設定処理がない | 実装予定がなければメニュー表示を整理 |

## 詳細メモ

* マイページの履歴・管理系UI
  * 該当UI: `frontend/src/pages/user/profile.tsx`
  * 実装済みに見える部分: `出品中` は `/user/items`、`購入履歴` は `/orders?role=buyer`、`販売履歴` は `/orders?role=seller` に接続している。
  * 未実装に見える部分: `入札中` と `ポイント履歴` は準備中表示。`住所変更` はクリック処理なし。自己紹介編集はtextarea表示のみで保存処理なし。
  * APIや状態管理との接続有無: `fetchHistory` は注文と出品一覧のみ取得。ユーザー入札一覧・ポイント履歴・自己紹介更新に対応するNext API/バックエンドルートは確認できなかった。
  * 今後実装する場合に確認すべきこと: 履歴の定義、取引中と履歴の分離、ポイント履歴の発生イベント、住所管理をプロフィールで扱うか購入フローだけにするか。
* 商品詳細・入札・オファー
  * 該当UI: `frontend/src/pages/items/[id]/index.tsx`
  * 実装済みに見える部分: `入札する` は `/items/:id/bids`、`値段交渉` は `/items/:id/offers` に接続。バックエンドにも `BidsController` と `OffersController` がある。
  * 未実装に見える部分: 自分の入札一覧、オークション終了後の落札/注文化、出品者向けオファー一覧・承認導線がない。
  * APIや状態管理との接続有無: 商品単位の作成APIはあり。ユーザー単位の履歴/管理APIは未確認。
  * 今後実装する場合に確認すべきこと: オークション終了処理をジョブで行うか、閲覧時に判定するか。オファー承認後の価格・注文作成フロー。
* 購入・取引・評価
  * 該当UI: `frontend/src/pages/items/[id]/checkout.tsx`, `frontend/src/pages/transaction/[id]/index.tsx`, `frontend/src/pages/transaction/[id]/rating.tsx`
  * 実装済みに見える部分: ポイント決済、住所CRUD、注文作成、取引メッセージ、取引ステータス更新。
  * 未実装に見える部分: クレジットカードは選択不可。評価画面は評価値とコメントを入力できるが、送信されるのは注文完了ステータスだけ。
  * APIや状態管理との接続有無: 注文/メッセージAPIは接続あり。評価API/モデルは見当たらない。
  * 今後実装する場合に確認すべきこと: 評価を保存する必要があるか、評価コメントをどこに表示するか。
* いいね・通知・認証補助
  * 該当UI: `frontend/src/pages/favorites.tsx`, `frontend/src/pages/notifications/index.tsx`, `frontend/src/pages/auth/forgot-password.tsx`
  * 実装済みに見える部分: いいね一覧は `/items` の `is_favorited` で絞り込み表示。
  * 未実装に見える部分: 閲覧履歴、いいねページのフィルタ、通知一覧/タブ、パスワード再設定送信処理。
  * APIや状態管理との接続有無: 閲覧履歴・通知・パスワード再設定用のNext APIは確認できなかった。
  * 今後実装する場合に確認すべきこと: 通知の発生元、閲覧履歴をローカル保存にするかサーバー保存にするか、再設定メール送信の運用。
* 管理画面
  * 該当UI: `admin/src/pages/dashboard.vue`, `orders.vue`, `settings.vue`, `admin/src/components/layouts/AdminLayout.vue`
  * 実装済みに見える部分: ユーザー、カテゴリ、商品、ダッシュボードの基本統計は管理APIに接続。
  * 未実装に見える部分: 注文管理ページ、売上推移グラフ、最近の活動、トップバー通知/検索、設定画面の更新処理。
  * APIや状態管理との接続有無: 管理注文用 `admin/v1/orders` ルートは確認できない。ダッシュボードの成長率はバックエンドで固定値。
  * 今後実装する場合に確認すべきこと: 管理者権限別の表示制御、注文管理で必要な操作範囲、設定画面を本当に管理画面内で提供するか。

## 今回は対応しないこと

* 機能実装
* API追加
* バックエンド変更
* DB変更
* 大幅なUI変更
